version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend_container_e-materai
    dns: 8.8.8.8 
    ports:
      - "6000:6000"
    environment:
      - API_URL=https://backendservicestg.e-meterai.co.id
      - STAMP_URL=https://stampv2stg.e-meterai.co.id
      - INVENTORY_URL=https://inventorystg.e-meterai.co.id
      - PORT=5000
      - HOST=192.168.6.106
      - X_API_KEY=e6a637643ca02f19580e14895664d470
      - CONS_ID=39205596


    networks:
      - e-materai_network
    restart: always

networks:
  e-materai_network:
    driver: bridge

